
> [!NOTE]
> **此文件内容已被老弟全量结构化迁移至 LanceDB Pro 向量数据库 (2026-04-09)。**
> 您的原始记录原封不动，但请勿在此继续手动追加。底层系统已接管全自动捕捉与混合检索。

### AI 内容生成 SOP (2026-03-27) - **补充教训**
- **重大教训 - 必须遵循官方文档**: 在调用 Google Veo 的 `generate_videos` API 时，我遗漏了一个关键的 `config` 参数，特别是 `config=types.GenerateVideosConfig(aspect_ratio="9:16")`。
- **问题根源**: 官方文档和示例明确指出需要此参数来指定视频的宽高比。我的代码中缺少这个参数，很可能是导致API虽然接受请求但无法正确处理、最终返回空视频数据的根本原因。
- **核心原则**: 在集成任何新的第三方API时，严禁想当然或只参考部分资料。**必须第一时间找到并严格遵循官方最新的代码示例（Code Example）**，确保所有必需的参数都已正确提供。这能避免大量无效的调试。

### AI 视频生成 - API 迭代与兼容处理 (2026-03-28)
- **废弃库的平滑迁移 (google.generativeai 到 google.genai)**: 
  - 当第三方库（如 Gemini）发出 `FutureWarning: All support for the package has ended` 时，不要试图在旧包的底层结构中盲目 `import` （例如寻找 `operations` 类）。
  - **处理方案**：立刻舍弃旧包的修复，去官方的 "Deprecated" 通知、官方的 Cookbook 仓库或者 PyPI (用 DuckDuckGo 无验证搜索代替 Google 被拦截的搜索) 找到全新的重构版本库。
  - **新库的变更特征**：`google.genai` 的调用方式已全面转向面向对象的 `Client` 架构（例如 `genai.Client(api_key=...)`，后续方法挂载在 `client.models`、`client.operations` 下），不再支持零散模块的直接挂载和平铺传参（所有文件数据、对象现在都被打包在如 `GenerateVideosSource` 之类的专属类中）。
- **Google Veo 审核与下载排坑**: 
  - **假视频（文件损坏）陷阱**: 如果生成视频下载后被 ffmpeg 报 `moov atom not found` 或非 MP4 格式，一定是拿到了**重定向的加密下载链接 (`uri`)**，却没有使用含有 `x-goog-api-key` 的 headers 进行鉴权请求（导致下到的是 401 报错页面的 HTML，却被当成了视频写入）。
  - **敏感词封锁 (`code: 3`)**: Veo 的预览版 API 审核极其苛刻（即便是儿童童话，出现"女孩"、“连衣裙”也会被 `Responsible AI` 直接拦截）。
  - **应对策略**: 不要死磕具体的人物/服装描写，直接转换为大场景的**风光空镜**、**花草意象**描写，这是最稳妥的通过方案。
- **排障最佳实践**: 
  - 遇到库的参数不认识（例如报没有某个属性），不要猜。直接通过 `exec` 用 Python 脚本打印其属性列表（如 `print(dir(obj))`、`print(inspect.signature(func))` 或 `print(obj.model_fields.keys())`）来解剖实际运行时对象的下划线格式还是驼峰格式（如 `videoBytes` 还是 `video_bytes`）。
  - 对于合并文件的文本清单（如 ffmpeg 的 `concat` 的 `filelist.txt`），写换行符时严防把 `\n` 误写成 `\\n`（变成字面上的 `\nfile`），这会直接导致读取到畸形路径。

### Weekly Review (2026-04-12)
- **记忆体系完成切换，但文档规则仍有惯性残留**：本周明确完成了从手工 Markdown 记忆到 `memory-lancedb-pro` 的认知切换。`MEMORY.md` 现仅作为归档说明，不应再作为日常追加主表。后续凡是“周报/心跳/启动”类规则，都应优先围绕自动记忆与检索设计，避免出现“底层自动化 + 上层手工维护”双轨并存。
- **模型配置的真实风险点不是 provider，而是 agent 覆盖与默认值打架**：多次对话表明，用户真正踩坑的是默认模型、agent override、前端临时切换三者互相干扰。经验是：若要稳定运行，应尽量收敛成单一默认模型；若允许切换，必须显式说明 agent 是否允许 override，并减少 UI 暴露的无效模型项。
- **自定义 OpenAI 兼容源接入流程已跑通**：新增自定义 provider 时，关键是统一 `models.providers.<name>`、`agents.defaults.model.primary` 与 agent 可用模型集合，避免只改 provider 不改 agent 绑定，导致“看起来配好了，实际仍回退默认模型”。
- **即梦电影化工作流新增两条硬规范**：一是首尾帧/分镜提示词必须写成电影级分镜说明，详细描述人物、环境、场景变化、镜头运动与光影效果，不能再用简略提示词；二是任何会触发扣费或长耗时生成的流程，必须先把剧本/提示词发给用户确认，未经确认不得直接执行生成。
- **长任务的核心体验问题是“中途静默”**：本周最明确的用户反馈不是最终结果错误，而是中间关键节点未主动播报。结论是：对 `直接执行` 模式，不能只在完成时回报，必须在“首帧成功、尾帧成功、视频提交、视频成功、后期处理中、最终成片完成”等状态变化时立即查询结果并主动汇报。
- **SOP 固化要双落地：skill + 桌面文档**：仅在 skill 中修改规则不够，用户明确要求时，还需要把“任务监测与回报机制”同步写入桌面 SOP，确保后续换工具、换会话、换执行端时也能继承同一套流程约束。
- **App Factory MVP 已打通真实导出闭环**：`Word -> PPT` 工厂当前已能完成本地项目创建、`ppt-master` 工程生成、模板资产复制、4 页 MVP SVG 生成及可编辑 PPTX 导出。当前瓶颈不在导出，而在真实 `.docx` 解析与从 Markdown 驱动内容化页面生成，下一阶段应把“固定 4 页”升级为“解析驱动 8-12 页”。
- **换开发工具时必须同步交接文档**：本周有明确场景要求把当前进度、操作流程和未来想法沉淀到本地 `HANDOFF.md`，以便切换 IDE/开发环境时不中断。今后凡是项目切换工具链或交接给下一个 Agent/开发器，都应默认生成一份本地 handoff 文档。
### Weekly Review (2026-04-26)
- **本周新增的有效记忆来源非常少，暴露出“周回顾依赖旧版 memory/ 日记文件”这一机制已经失效**：最近 7 天内，`memory/` 目录里实际只有 2026-04-24 的三份会话摘要，没有 04-20～04-26 的日更日志。结论是，既然系统已切到 `memory-lancedb-pro` 自动记忆，周报/复盘类 cron 不能再假设 `memory/YYYY-MM-DD.md` 会持续产出，否则会频繁读空或误判“无新信息”。
- **模型收敛策略已从“多模型切换”转为“单模型统一”并落地**：本周最明确的新变化是用户要求“只留下 `codexzh/gpt-5.4`，默认都是这个”，随后网关已完成重启与配置应用。这说明后续任何排障、默认值解释、界面提示和 SOP，都应以“单一默认模型”作为新基线，而不是继续沿用早期多模型并存的思路。
- **ClawLink Console 的高频任务形态已经收敛为“网页风格仿制流水线”**：4 月 24 日多次连续任务都围绕同一模式展开：打开目标站 → 提炼设计语言 → 生成 HTML/CSS 新页面 → 落 `design-notes.md` → 生成 `preview.png`。这表明该工作流已具备 SOP 价值，后续应沉淀为固定模板，减少重复口头指令和中间沟通成本。
- **在 Console/问答混合场景里，最容易犯的错不是执行失败，而是“上下文切换后回答跑偏”**：本周出现了用户明确指出“你又忘了读取记忆文件和身份文件了”，说明当对话从执行任务跳到理解型问答（如“你都吸收了什么”“hermes 是什么”）时，若不先回到身份/记忆基线，回答就会被局部上下文带偏。今后凡是这类元问题，必须先按启动基线对齐后再答。
- **重复执行型任务需要更强的“模板化复用”，而不是每次从头解释**：同一天内多次收到几乎相同的 UI 仿制任务，仅目录和网址不同。这说明后续应该抽象出统一执行模板与交付清单，避免在重复任务里消耗额外 token 和心智负担。
### Weekly Review (2026-05-03)
- **本周未从 `memory/` 目录发现新的日更或问题复盘文件**：最近 7 天内没有可供读取的新增 `memory/` 文件，因此没有从旧版 Markdown 日志中提炼出新的显著学习、问题复盘或用户反馈。
- **这再次确认当前周回顾机制与实际记忆体系脱节**：系统已切换到 `memory-lancedb-pro` 自动记忆，但本 cron 仍依赖 `memory/` 目录与 `MEMORY.md` 的旧流程，导致周回顾在多数周都会读空。后续若要让周回顾继续有效，应改为直接检索自动记忆系统，而不是继续假设本地日记文件会持续更新。
- **本周结论**：无新的重大经验需要追加归档；本次回顾的主要发现，是“周回顾数据源需要升级”本身已经成为稳定性问题。

## TODO for 2026-03-23

- [ ] **Task:** Deploy an OpenClaw Skill to control an Elephant Robotics arm, incorporating vision and voice control.
- **Context:** Based on research from 2026-03-22, the plan is to use ROS 2 as a bridge for the arm. The user has added requirements for camera vision and voice commands.
- **Prerequisites:** User will provide the specific arm model and tech docs.
- **Components:**
    - **Manipulation:** Create a new Skill to interface with the arm via ROS 2.
    - **Vision:** Use the existing `camsnap` skill (or similar) to capture images from an external camera. The image will be analyzed by a vision model to provide context for the arm's actions.
    - **Voice:** Use the existing `openai-whisper` skill to transcribe voice commands into text prompts for OpenClaw.
- **Goal:** Build an integrated system where the user can speak a command, the system "sees" the workspace with a camera, and the robotic arm executes the task.

## Update (End of Session): Voice Assistant Completed
- Successfully built a Python voice assistant (`voice_assistant_final.py` saved to workspace).
- Uses Whisper (base model) for Wake Word ("老弟") + VAD + Command Transcription.
- Uses `subprocess` and `openclaw system event` to bridge transcribed commands into the OpenClaw agent's context.
- **Ready for tomorrow:** Connect this working voice pipeline to the Elephant Robotics arm via ROS 2.

## 明天(2026-03-24) 核心修复任务：抖音自动化视频质量与文案
1. **视频质量提升**：优化 Veo 3.1 的 Prompt 生成逻辑，增加高清、电影级光影等提示词；检查 FFmpeg 合并时的码率（Bitrate）和分辨率设置，拒绝高糊画质。
2. **文案缺失Bug修复**：目前的 Playwright 自动发布脚本漏掉了“填写简介文案”的步骤。明天需要重构 `publish-video.mjs` 中的输入逻辑，确保 AI 生成的文案和 #话题 能够精准填入抖音的编辑框中。

## 明天视频制作终极标准
老板已通过《赛博朋克演示视频》的质量验收。
1. **画质与渲染标准**：Veo 3.1 提示词必须附带电影级画质强化 (`cinematic lighting, 8k resolution, photorealistic, ultra-detailed`)。
2. **声音与字幕标准**：继续使用 `composite-video.js` 方案：采用高清男声（Edge TTS 晓晓/云希），配合自带透明黑底阴影的高级硬核字幕（`.srt`烧录）。
3. **内容**：明天实战时，将测试文案全部替换为“真实的每日AI科技前沿资讯”，生成完整版播报视频。
